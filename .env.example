# Database
DATABASE_URL="postgresql://user:password@host:port/database?schema=public"

# Application
NODE_ENV="development"
PORT=3000

# Redis (Event Bus & Cache)
# Para usar con Docker Compose: docker compose up -d redis
# El servicio estará disponible en redis:6379 dentro de la red de Docker
# Si corres la app fuera de Docker, usa localhost:6379
REDIS_HOST=redis
REDIS_PORT=6379
# REDIS_PASSWORD=your_redis_password  # Opcional - no configurado en desarrollo
REDIS_DB=0

# Si corres Redis localmente (sin Docker), usa:
# REDIS_HOST=localhost
